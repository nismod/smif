sector_models:
  - name: water_supply
    path: ../models/water_supply.py
    classname: WaterSupplySectorModel
    config_dir: ../data/water_supply
timesteps: timesteps.yaml
planning:
  pre_specified:
    use: false
    files:
      - expected_to_2020.yaml
      - national_infrastructure_pipeline.yaml
  rule_based:
    use: false
  optimisation:
    use: false
